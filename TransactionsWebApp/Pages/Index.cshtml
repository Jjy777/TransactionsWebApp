@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="text-center">
  @*  <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>*@
</div>
<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Transactions</h2>
        </div>
          <div class="col-6">
        </div>
    </div>
    <br />
    <div id="UploadDiv">
        <div class="row pt-4">
            <div>
                <p class="text-secondary">Upload file</p>
            </div>
            <div>
                <form method="post" enctype="multipart/form-data">
                    <div>
                        <input id="fileInput" type="file" name="file" class="form-control"/>
                    </div>
                    <br />
                    <div class="mb-3">
                        <input class="btn btn-primary" type="submit" value="Upload" />
                    </div>
                </form>
            </div>
        </div>
    </div>
    <br />
    <div>
        <table class="table table-bordered table-striped" width="100%" >
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Payment</th>
                    <th>Status</th>
                </tr>
            </thead>
            <tbody>
                @foreach(var obj in Model.Transactions)
                {
                    <tr>
                        <td width="30%">@obj.TransactionID</td>
                        <td width="50%">@String.Format("{0:#,##0.00}",@obj.Amount) @obj.CurrencyCode</td>
                        <td width="10%">@obj.Status</td>
                    </tr>
                    
                }
            </tbody>
        </table>
    </div>
</div>
<script>
    //var uploadField = document.getElementById("fileInput");
    //var maxFileSize = 1024;

    //uploadField.onchange = function () {
    //    if (this.files[0].size > maxFileSize) {
    //        alert("Maximum allowed file size is 1 MB.");
    //        this.value = "";
    //    };
    
</script>
